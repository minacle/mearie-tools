# create_file

파일을 생성하는 함수 도구입니다.

## 입력

**JSON**:

- `path` (_string_): 생성할 파일의 절대 경로입니다.

## 출력

**평문**: 생성한 파일의 절대 경로입니다.

## 동작

지정된 절대 경로에 파일을 생성하고, 그 파일의 절대 경로를 반환합니다.

상위 디렉터리가 존재하지 않는 경우, 오류로 처리합니다.

오류 시, 아무것도 출력하지 않습니다.
